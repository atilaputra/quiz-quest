name: Deploy PHP App to Azure

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Login to Azure Container Registry
      uses: docker/login-action@v2
      with:
        registry: ${{ secrets.ACR_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}
    
    - name: Build and push Docker image
      run: |
        docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/quiz-quest:${{ github.sha }} .
        docker build -t ${{ secrets.ACR_LOGIN_SERVER }}/quiz-quest:latest .
        docker push ${{ secrets.ACR_LOGIN_SERVER }}/quiz-quest:${{ github.sha }}
        docker push ${{ secrets.ACR_LOGIN_SERVER }}/quiz-quest:latest
    
    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        images: ${{ secrets.ACR_LOGIN_SERVER }}/quiz-quest:${{ github.sha }}
